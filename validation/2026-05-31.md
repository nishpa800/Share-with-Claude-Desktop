# Data validation — 2026-05-31 10:00 CST

> **CORRECTION (post-supervisor verification, appended 10:?? CST).** Two findings below were revised after the supervisor cross-referenced sources this agent did not scan:
> 1. **NOI permanent loss is 1 day (05/18), NOT 3.** A second, unmanaged incumbent capturer `capture-ws.py` (PID 50499, quant-brain) writes NOI to `/Users/anishpatel/quant-brain/data/realtime/NOI/` — OUTSIDE the OWC drive this agent scans. It captured **05/26 (430 files)** and **05/27 (617 files)**, which are recoverable. Only **05/18** is truly lost. *Scan blind spot: NOI exists in two locations; this agent only walked `OWC/massive-realtime/stocks/noi/`. Future runs must also check the quant-brain mirror.*
> 2. **Capturer down — true root cause (3 layers):** (A) installed `com.anish.massive-realtime` plist ran `uv run` with **no `MASSIVE_API_KEY` in env** → exit 78 before any log; (B) the correct `com.anish.multistream` plist couldn't create stdio logs on the OWC volume (space-in-path/ext-vol perms) — logs moved to `~/Library/Logs/anish-capturer/`; (C) Massive permits **one `/stocks` WS connection**, so the managed agent `1008`-looped against the incumbent. Supervisor parked the managed agent for the weekend; NOI is flowing via incumbent `capture-ws.py`. **Pending Anish approval before Mon 08:30 CST:** `kill 50499 && launchctl load -w ~/Library/LaunchAgents/com.anish.multistream.plist`.
> 3. **Batch poller root cause:** `agents/run-paid-puller.sh:44` calls a deleted `poller/rest_poller.py` (refactored into `sources/*/scripts/`); wrapper swallows the error as rc=0 → the 05/15 freeze. Code repoint + replay, recoverable.
> Supervisor dashboard: `/Users/anishpatel/code/anish/trading-data-system/STATE_OF_THE_UNION.md`


**Agent:** data-extractor · **Run type:** weekend sweep (Saturday)
**Drive:** OWC Envoy Ultra 4 TB — MOUNTED, read-only scan
**Trading-day calendar context:** 2026-05-25 was Memorial Day (market closed). Last 10 market days: 05/15, 05/18, 05/19, 05/20, 05/21, 05/22, 05/26, 05/27, 05/28, 05/29.

## Summary
- Data sources checked: 5 (realtime NOI, realtime LULD, S3 flatfiles, REST backfills, WhaleWisdom)
- Anomalies found: 6
- **Critical (irrecoverable WS loss / capturer down): 2**
- Warning (stale-but-recoverable / drift): 3
- Info: 3

## Critical anomalies

| Source | Dataset | Issue | Hours/Days offline | Recoverable? | Action taken |
|---|---|---|---|---|---|
| massive | NOI (WS, `stocks/noi/`) | Missing whole trading days **05/18, 05/26, 05/27**. Present: 05/19–22, 05/28–29. | 3 market days | **NO — WS-only, $49/m, permanently lost** | **Paged supervisor.** |
| massive | NOI capturer (`com.anish.massive-realtime` launchd) | **NOT LOADED** in launchctl. No `launchd_massive_realtime.{out,err}.log` ever written. Existing NOI 05/28–29 came from ad-hoc `noi_sdk_screen` runs, not the persistent agent → forward capture is **dead right now**. | Ongoing | Every minute the market is open with this unloaded = unrecoverable NOI loss | **Paged supervisor** to `launchctl load` the plist before Monday 08:30 CST open. |

## Warnings

| Source | Dataset | Issue | Severity |
|---|---|---|---|
| massive | S3 flatfiles (all `us_stocks_sip/*`, `us_options_opra/*`) | Latest file = **2026-05-15**. ~9 trading days behind (missing 05/18–05/29). gzip integrity OK, header schema matches KNOWN_ENDPOINTS. | WARN — recoverable via S3 backfill, but pipeline is clearly stalled since 05/15. |
| massive | REST backfills (`benzinga_earnings`, `benzinga_news`, `etf_fund_flows`, `dividends`, `short_volume`, `splits`) | All latest = **2026-05-15**. Same ~16-calendar-day stall as flatfiles → suggests a single shared poller/cron died on/after 05/15. | WARN — recoverable via REST replay. |
| massive | REST `etf_constituents` | Latest = **2026-01-28** (4 months stale). | WARN — lower urgency (quarterly-grain reference), but confirm refresh policy. |

## Info / no-action

| Source | Dataset | Note |
|---|---|---|
| massive | LULD (`stocks/luld/`, latest 05/22) | Per `sources/massive/KNOWN_ENDPOINTS.md`, **Anish is NOT subscribed to LULD** (bolt-on he doesn't have). Capture cessation is **not a data loss**. Recommend stopping the LULD subscribe to avoid wasted WS bandwidth. NOT escalated. |
| massive | Empty flatfile asset classes | `us_indices`, `global_crypto`, `global_forex`, `us_futures_cme` dir trees exist but contain **0 files**. Never backfilled; not core to the VOB equities/options thesis. |
| massive | Legacy path `massive-realtime/noi/2026/05/19/noi_0152.jsonl.gz` | Single stale file under the OLD path layout, superseded by `stocks/noi/`. Cleanup candidate. |

## Schema / integrity checks (passed)
- **NOI jsonl** (`stocks/noi/2026/05/29/1030.jsonl`): keys = `auction_time, auction_type, book_clearing_price, captured_at, event_type, exchange_id, imbalance_quantity, paired_quantity, symbol, symbol_sequence, time_stamp` — matches documented NOI fields (`imbalance_quantity, paired_quantity, book_clearing_price, auction_type`). No drift.
- **Flatfile day_aggs header**: `ticker,volume,open,close,high,low,window_start,transactions` — matches KNOWN_ENDPOINTS. No partial gzip (`gzip -t` → OK).
- No zero-byte files found in sampled partitions.

## Per-source recency table

| Source | Latest captured | Expected (as of 05/31) | Gap |
|---|---|---|---|
| realtime NOI | 2026-05-29 | 2026-05-29 (last mkt day) | OK on tail; **3 interior days missing** |
| realtime LULD | 2026-05-22 | n/a (unsubscribed) | N/A |
| flatfiles us_stocks_sip | 2026-05-15 | 2026-05-29 | ~9 trading days |
| flatfiles us_options_opra | 2026-05-15 | 2026-05-29 | ~9 trading days |
| REST (core endpoints) | 2026-05-15 | ~2026-05-29 | ~9 trading days |
| REST etf_constituents | 2026-01-28 | recent | ~4 months |
| WhaleWisdom | `quarters.parquet` only | next window Aug mid-month | N/A this run |

## Root-cause hypothesis (for supervisor)
A shared ingestion stall began **on/around 2026-05-15** (flatfiles + all REST endpoints freeze on the same date). Independently, the **NOI persistent capturer never ran under launchd** — NOI only has data on days someone hand-started `noi_sdk_screen`. Two separate failures, one date-correlated batch poller + one never-loaded WS agent.

## Next-run focus
1. Confirm supervisor reloaded `com.anish.massive-realtime` and NOI is live before 2026-06-01 08:30 CST open.
2. Verify the 05/15 batch-poller stall is fixed and flatfiles/REST backfill 05/18–05/29.
3. Re-scan NOI interior-day completeness after backfill (interior gaps are forward-only — confirm no further new gaps).
